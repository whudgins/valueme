<%= form_for(@possession) do |f| %>
  <% if @possession.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@possession.errors.count, "error") %> prohibited this possession from being saved:</h2>

      <ul>
      <% @possession.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :model_num %><br>
    <%= f.text_field :model_num %>
  </div>
  <div class="field">
    <%= f.label :receipt_url %><br>
    <%= f.text_field :receipt_url %>
  </div>
  <div class="field">
    <%= f.label :est_value %><br>
    <%= f.text_field :est_value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
